version: '3.8'

services:
  signaling-server:
    build:
      context: .
      dockerfile: packages/signaling-server/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    container_name: voice-chat-signaling
